{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PinchManifest",
  "description": "Root configuration manifest for a Pinch project (`pinch.yaml`).",
  "type": "object",
  "properties": {
    "auto_restart": {
      "description": "Whether processes restart automatically if they exit or crash (default: `true`).",
      "type": [
        "boolean",
        "null"
      ]
    },
    "auto_start": {
      "description": "Whether processes start automatically upon launching the supervisor (default: `true`).",
      "type": [
        "boolean",
        "null"
      ]
    },
    "docker_networks": {
      "description": "Custom Docker bridge networks managed and initialized by Pinch.",
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": {
        "$ref": "#/$defs/DockerNetworkConfig"
      }
    },
    "grace_period": {
      "description": "Delay in milliseconds before automatically restarting a process after an exit (default: `3000`).",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint64",
      "minimum": 0
    },
    "layout": {
      "description": "Progressive edge-carving layout rules defining how process panes are arranged.",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/$defs/LayoutBlock"
      }
    },
    "logs_max_size": {
      "description": "Maximum number of log lines to retain in memory per process pane.",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint",
      "minimum": 0
    },
    "processes": {
      "description": "List of supervised processes, containers, and background services to execute.",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/$defs/ProcessManifest"
      }
    },
    "project": {
      "description": "Core project metadata, governance attributes, and architecture classification.",
      "$ref": "#/$defs/ProjectManifest"
    },
    "shell": {
      "description": "If true, executes shorthand command strings using `bash -c` globally (default: `false`).",
      "type": [
        "boolean",
        "null"
      ]
    },
    "vars": {
      "description": "Custom configuration variables available for string expansion (e.g., `{{var_name}}`).",
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": {
        "type": "string"
      }
    },
    "watch_settle_time_ms": {
      "description": "Debounce delay in milliseconds when watching files for auto-restarting (default: `800`).",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint64",
      "minimum": 0
    }
  },
  "additionalProperties": false,
  "required": [
    "project"
  ],
  "$defs": {
    "AuthType": {
      "description": "Primary authentication mechanism for ingress requests.",
      "oneOf": [
        {
          "description": "Identity provider.",
          "type": "string",
          "const": "idp"
        },
        {
          "description": "Generic OAuth 2.0.",
          "type": "string",
          "const": "oauth2"
        },
        {
          "description": "Generic SAML.",
          "type": "string",
          "const": "saml"
        },
        {
          "description": "Mutual TLS client certificate authentication.",
          "type": "string",
          "const": "mtls"
        },
        {
          "description": "Custom token authentication.",
          "type": "string",
          "const": "token"
        },
        {
          "description": "HTTP Basic authentication (legacy/internal).",
          "type": "string",
          "const": "basic"
        },
        {
          "description": "Custom form authentication.",
          "type": "string",
          "const": "form"
        },
        {
          "description": "Unprotected public endpoint (no authentication required).",
          "type": "string",
          "const": "none"
        }
      ]
    },
    "DockerNetworkConfig": {
      "description": "Configuration for creating a Docker bridge network.",
      "anyOf": [
        {
          "description": "CIDR subnet string for the bridge network (e.g., `\"172.18.23.0/24\"`).",
          "type": "string"
        },
        {
          "description": "Explicit network configuration specifying subnet CIDR and custom Docker creation arguments.",
          "type": "object",
          "properties": {
            "args": {
              "description": "Custom CLI arguments passed to `docker network create`.",
              "type": [
                "array",
                "null"
              ],
              "items": {
                "type": "string"
              }
            },
            "subnet": {
              "description": "CIDR subnet string for the bridge network.",
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "subnet"
          ]
        }
      ]
    },
    "ExposureType": {
      "description": "Network ingress reachability for the project.",
      "oneOf": [
        {
          "description": "Reachable from the public internet or Cloudflare edge.",
          "type": "string",
          "const": "internet"
        },
        {
          "description": "Reachable only inside the company VPN, VPC, or internal service mesh.",
          "type": "string",
          "const": "internal"
        },
        {
          "description": "No incoming network traffic (e.g., background worker or CLI tool).",
          "type": "string",
          "const": "none"
        }
      ]
    },
    "LayoutBlock": {
      "description": "A progressive edge-carving block ruleset for arranging terminal panes.",
      "type": "object",
      "properties": {
        "direction": {
          "description": "Split orientation for sub-panes (`horizontal` or `vertical`).",
          "type": [
            "string",
            "null"
          ]
        },
        "edge": {
          "description": "Side of the remaining terminal space to carve from (`top`, `bottom`, `left`, `right`).",
          "$ref": "#/$defs/LayoutEdge"
        },
        "size_percentage": {
          "description": "Percentage of currently available space to allocate (0 to 100).",
          "type": "integer",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        "splits": {
          "description": "Sub-panes to arrange within this carved edge block.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/LayoutSplit"
          }
        },
        "title": {
          "description": "Target process title to place inside this block (or `\"Combined Logs\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "unassigned": {
          "description": "If true, automatically places all unassigned process panes inside this block.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "edge",
        "size_percentage"
      ]
    },
    "LayoutEdge": {
      "description": "Target edge of the terminal space to carve from.",
      "oneOf": [
        {
          "description": "Carve from the top edge.",
          "type": "string",
          "const": "top"
        },
        {
          "description": "Carve from the bottom edge.",
          "type": "string",
          "const": "bottom"
        },
        {
          "description": "Carve from the left edge.",
          "type": "string",
          "const": "left"
        },
        {
          "description": "Carve from the right edge.",
          "type": "string",
          "const": "right"
        }
      ]
    },
    "LayoutSplit": {
      "description": "A sub-pane division within an edge-carved layout block.",
      "type": "object",
      "properties": {
        "size_percentage": {
          "description": "Percentage of space within the parent block to allocate (0 to 100).",
          "type": "integer",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        "title": {
          "description": "Target process title to place inside this split (or `\"Combined Logs\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "unassigned": {
          "description": "If true, automatically places all unassigned process panes inside this split.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "size_percentage"
      ]
    },
    "LifecycleType": {
      "description": "Operational maintenance status of the repository.",
      "oneOf": [
        {
          "description": "Actively developed and supported in production.",
          "type": "string",
          "const": "active"
        },
        {
          "description": "Scheduled for decommissioning; do not add new dependencies.",
          "type": "string",
          "const": "deprecated"
        },
        {
          "description": "In production but receiving only critical bug/security fixes.",
          "type": "string",
          "const": "maintenance"
        },
        {
          "description": "Experimental proof-of-concept; no production SLA.",
          "type": "string",
          "const": "prototype"
        }
      ]
    },
    "PaneMode": {
      "description": "Display rendering mode for a process pane.",
      "oneOf": [
        {
          "description": "Standard streaming log tailer with wrap and truncation controls.",
          "type": "string",
          "const": "log"
        },
        {
          "description": "Allocates a PTY for interactive terminal applications (`top`, `vim`, `htop`).",
          "type": "string",
          "const": "tui"
        }
      ]
    },
    "ProcessManifest": {
      "description": "Configuration for an individual supervised process or container.",
      "type": "object",
      "properties": {
        "auto_restart": {
          "description": "Override global auto-restart default for this specific process.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "auto_start": {
          "description": "Override global auto-start default for this specific process.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "cwd": {
          "description": "Working directory for process execution (supports variable expansion like `{{pwd}}`).",
          "type": [
            "string",
            "null"
          ]
        },
        "grace_period": {
          "description": "Override global grace period delay in milliseconds for this specific process.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "link": {
          "description": "Optional web URL or link associated with this process (clickable in the TUI header).",
          "type": [
            "string",
            "null"
          ]
        },
        "mode": {
          "description": "Display mode for the process output in the TUI dashboard (`log` or `tui`).",
          "anyOf": [
            {
              "$ref": "#/$defs/PaneMode"
            },
            {
              "type": "null"
            }
          ]
        },
        "run": {
          "description": "Execution definition specifying how the command or container is spawned.",
          "$ref": "#/$defs/RunManifest"
        },
        "title": {
          "description": "Display name of the process pane in the TUI dashboard.",
          "type": "string"
        },
        "watch": {
          "description": "List of file or directory paths that trigger an automatic restart when modified.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "watch_settle_time_ms": {
          "description": "Process-specific debounce delay in milliseconds for file watching.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "title",
        "run"
      ]
    },
    "ProjectManifest": {
      "description": "High-level project identification, governance, and architecture classification.",
      "type": "object",
      "properties": {
        "auth": {
          "description": "Primary authentication mechanism for ingress requests.",
          "anyOf": [
            {
              "$ref": "#/$defs/AuthType"
            },
            {
              "type": "null"
            }
          ]
        },
        "environments": {
          "description": "List of deployment environments supported by this project (e.g., `dev`, `staging`, `prod`).",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "exposure": {
          "description": "Network ingress reachability for the project.",
          "anyOf": [
            {
              "$ref": "#/$defs/ExposureType"
            },
            {
              "type": "null"
            }
          ]
        },
        "lifecycle": {
          "description": "Operational maintenance status of the repository.",
          "anyOf": [
            {
              "$ref": "#/$defs/LifecycleType"
            },
            {
              "type": "null"
            }
          ]
        },
        "name": {
          "description": "Human-readable project title displayed at the top of the TUI dashboard.",
          "type": "string"
        },
        "sensitivity": {
          "description": "Sensitive data classifications handled by the service.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/Sensitivity"
          }
        },
        "type": {
          "description": "Primary architectural role of the project.",
          "anyOf": [
            {
              "$ref": "#/$defs/ProjectType"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "ProjectType": {
      "description": "Primary architectural role of the project.",
      "oneOf": [
        {
          "description": "Shared code dependency imported by other projects.",
          "type": "string",
          "const": "library"
        },
        {
          "description": "Long-running server, API, or backend daemon.",
          "type": "string",
          "const": "service"
        },
        {
          "description": "CLI utility or internal developer tool.",
          "type": "string",
          "const": "tool"
        },
        {
          "description": "Short-lived batch process, cron, or CI/CD script.",
          "type": "string",
          "const": "job"
        }
      ]
    },
    "RunKind": {
      "description": "Explicit runtime environment type for a supervised process.",
      "oneOf": [
        {
          "description": "Spawns a standard local OS process.",
          "type": "object",
          "properties": {
            "bash": {
              "description": "Whether to wrap the command string in `bash -c` (default: `false`).",
              "type": "boolean",
              "default": false
            },
            "cmd": {
              "description": "The command string to execute.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "process"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "cmd"
          ]
        },
        {
          "description": "Spawns a managed Docker container using `docker run --rm`.",
          "type": "object",
          "properties": {
            "args": {
              "description": "Arguments passed to the container's entrypoint.",
              "type": [
                "string",
                "null"
              ]
            },
            "image": {
              "description": "The Docker image reference to run.",
              "type": "string"
            },
            "opts": {
              "description": "Arguments passed to `docker run --rm` (e.g., `--name`, `--network`, `--ip`).",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "type": "string",
              "const": "docker"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "image"
          ]
        },
        {
          "description": "Spawns a local host binary attached inside a Docker bridge network namespace (`docker-intrude`).",
          "type": "object",
          "properties": {
            "bash": {
              "description": "Whether to wrap the command string in `bash -c` (default: `false`).",
              "type": "boolean",
              "default": false
            },
            "cmd": {
              "description": "Host command string to execute within the target network namespace.",
              "type": "string"
            },
            "ip": {
              "description": "Static IP address assigned to the namespace container on the target bridge subnet.",
              "type": "string"
            },
            "network": {
              "description": "Target Docker network name (optional if exactly one network is defined in `docker_networks`).",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "type": "string",
              "const": "docker-intrude"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "ip",
            "cmd"
          ]
        }
      ]
    },
    "RunManifest": {
      "description": "How the process command is specified in YAML.",
      "anyOf": [
        {
          "description": "Simple shell command string executed as a local OS process.",
          "type": "string"
        },
        {
          "description": "Explicit execution configuration specifying process type (`process`, `docker`, or `docker-intrude`).",
          "$ref": "#/$defs/RunKind"
        }
      ]
    },
    "Sensitivity": {
      "description": "Sensitive data classifications handled by the service.",
      "oneOf": [
        {
          "description": "Non-sensitive data safe for public distribution.",
          "type": "string",
          "const": "public"
        },
        {
          "description": "Standard internal business data.",
          "type": "string",
          "const": "internal"
        },
        {
          "description": "Restricted company intellectual property or trade secrets.",
          "type": "string",
          "const": "confidential"
        },
        {
          "description": "Personally Identifiable Information (names, emails, addresses).",
          "type": "string",
          "const": "pii"
        },
        {
          "description": "Payment Card Industry data (credit cards, billing details).",
          "type": "string",
          "const": "pci"
        },
        {
          "description": "Protected Health Information (medical records, insurance claims).",
          "type": "string",
          "const": "phi"
        }
      ]
    }
  }
}