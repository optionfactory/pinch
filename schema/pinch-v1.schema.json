{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PinchManifest",
  "description": "Root configuration manifest for a Pinch project (`pinch.yaml`).",
  "type": "object",
  "properties": {
    "auto_restart": {
      "description": "Whether processes restart automatically if they exit or crash (default: `true`).",
      "type": "boolean"
    },
    "auto_start": {
      "description": "Whether processes start automatically upon launching the supervisor (default: `true`).",
      "type": "boolean"
    },
    "docker_networks": {
      "description": "Custom Docker bridge networks managed and initialized by Pinch.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/DockerNetworkConfig"
      }
    },
    "grace_period": {
      "description": "Delay in milliseconds before automatically restarting a process after an exit (default: `3000`).",
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    },
    "layout": {
      "description": "Progressive edge-carving layout rules defining how process panes are arranged.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/LayoutNode"
      }
    },
    "logs_max_size": {
      "description": "Maximum number of log lines to retain in memory per process pane.",
      "type": "integer",
      "format": "uint",
      "minimum": 0
    },
    "name": {
      "description": "Name of the project.",
      "type": "string",
      "pattern": "^[A-Za-z0-9-_]+$"
    },
    "processes": {
      "description": "List of supervised processes, containers, and background services to execute.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/ProcessManifest"
      }
    },
    "schema_version": {
      "description": "Explicit schema version for manifest compatibility (must be 1).",
      "type": "integer",
      "const": 1
    },
    "shell": {
      "description": "If present, executes commands using the configed shell '-c' option globally.",
      "$ref": "#/$defs/WrappingShell"
    },
    "vars": {
      "description": "Custom configuration variables available for string expansion (e.g., `{{var_name}}`).",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "watch_settle_time_ms": {
      "description": "Debounce delay in milliseconds when watching files for auto-restarting (default: `800`).",
      "type": "integer",
      "format": "uint64",
      "minimum": 0
    }
  },
  "additionalProperties": false,
  "required": [
    "schema_version",
    "name"
  ],
  "$defs": {
    "ContainerEngine": {
      "description": "Container engine binary used to spawn a container.",
      "type": "string",
      "enum": [
        "docker",
        "podman"
      ]
    },
    "DockerMountConfig": {
      "description": "Configuration for a single container mount.",
      "type": "object",
      "properties": {
        "opts": {
          "description": "Comma-separated extra mount options appended verbatim (e.g., `bind-propagation=rshared`).",
          "type": "string"
        },
        "readonly": {
          "description": "Whether the mount is readonly (default `true`).",
          "type": "boolean"
        },
        "source": {
          "description": "Source path of the mount.",
          "type": "string"
        },
        "target": {
          "description": "Target path inside the container (defaults to `source`).",
          "type": "string"
        },
        "type": {
          "description": "Mount type (default `bind`).",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "source"
      ]
    },
    "DockerNetworkConfig": {
      "description": "Configuration for creating a Docker bridge network.",
      "anyOf": [
        {
          "description": "CIDR subnet string for the bridge network (e.g., `\"172.18.23.0/24\"`).",
          "type": "string"
        },
        {
          "description": "Explicit network configuration specifying subnet CIDR and custom Docker creation arguments.",
          "type": "object",
          "properties": {
            "args": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "subnet": {
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "subnet"
          ]
        }
      ]
    },
    "LayoutDirection": {
      "description": "Split orientation for sub-panes (`horizontal` or `vertical`).",
      "type": "string",
      "enum": [
        "horizontal",
        "vertical"
      ]
    },
    "LayoutEdge": {
      "description": "Target edge of the terminal space to carve from.",
      "type": "string",
      "enum": [
        "top",
        "bottom",
        "left",
        "right"
      ]
    },
    "LayoutNode": {
      "description": "A recursive edge-carving and splitting node for arranging terminal panes.",
      "type": "object",
      "properties": {
        "direction": {
          "description": "Split orientation for child panes inside this node (`horizontal` or `vertical`).",
          "$ref": "#/$defs/LayoutDirection"
        },
        "edge": {
          "description": "Side of the remaining terminal space to carve from (`top`, `bottom`, `left`, `right`).",
          "$ref": "#/$defs/LayoutEdge"
        },
        "items": {
          "description": "Layout nodes to recursively arrange within this node.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/LayoutNode"
          }
        },
        "name": {
          "description": "Target process name to place inside this pane (or `\"combined-logs\"`).",
          "type": "string",
          "pattern": "^[A-Za-z0-9-_]+$"
        },
        "size": {
          "description": "Percentage of available space to allocate (0 to 100). Auto-calculated among siblings if omitted.",
          "type": "integer",
          "format": "uint16",
          "maximum": 100,
          "minimum": 0
        },
        "unassigned": {
          "description": "If true, automatically places all unassigned process panes inside this node.",
          "type": "boolean"
        }
      },
      "additionalProperties": false,
      "oneOf": [
        {
          "description": "Leaf node targeting a specific process name or 'combined-logs'",
          "not": {
            "anyOf": [
              {
                "required": [
                  "unassigned"
                ]
              },
              {
                "required": [
                  "items"
                ]
              }
            ]
          },
          "required": [
            "name"
          ]
        },
        {
          "description": "Leaf node acting as the container for unassigned process panes",
          "not": {
            "anyOf": [
              {
                "required": [
                  "name"
                ]
              },
              {
                "required": [
                  "items"
                ]
              }
            ]
          },
          "required": [
            "unassigned"
          ]
        },
        {
          "description": "Branch node splitting screen area recursively among child nodes",
          "not": {
            "anyOf": [
              {
                "required": [
                  "name"
                ]
              },
              {
                "required": [
                  "unassigned"
                ]
              }
            ]
          },
          "required": [
            "items"
          ]
        }
      ]
    },
    "PaneMode": {
      "description": "Display rendering mode for a process pane.",
      "type": "string",
      "enum": [
        "log",
        "tui"
      ]
    },
    "ProcessManifest": {
      "description": "Configuration for an individual supervised process or container.",
      "type": "object",
      "properties": {
        "auto_restart": {
          "description": "Override global auto-restart default for this specific process.",
          "type": "boolean"
        },
        "auto_start": {
          "description": "Override global auto-start default for this specific process.",
          "type": "boolean"
        },
        "cwd": {
          "description": "Working directory for process execution (supports variable expansion like `{{pwd}}`).",
          "type": "string"
        },
        "grace_period": {
          "description": "Override global grace period delay in milliseconds for this specific process.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "link": {
          "description": "Optional web URL or link associated with this process (clickable in the TUI header).",
          "type": "string"
        },
        "mode": {
          "description": "Display mode for the process output in the TUI dashboard (`log` or `tui`).",
          "$ref": "#/$defs/PaneMode"
        },
        "name": {
          "description": "Name of the process.",
          "type": "string",
          "pattern": "^[A-Za-z0-9-_]+$"
        },
        "run": {
          "description": "Execution definition specifying how the command or container is spawned.",
          "$ref": "#/$defs/RunManifest"
        },
        "title": {
          "description": "Display name of the process pane in the TUI dashboard (defaults to name).",
          "type": "string"
        },
        "watch": {
          "description": "List of file or directory paths that trigger an automatic restart when modified.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "watch_settle_time_ms": {
          "description": "Process-specific debounce delay in milliseconds for file watching.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "run"
      ]
    },
    "RunKind": {
      "description": "Explicit runtime environment type for a supervised process.",
      "oneOf": [
        {
          "description": "Spawns a standard local OS process.",
          "type": "object",
          "properties": {
            "cmd": {
              "description": "The command string to execute.",
              "type": "string"
            },
            "shell": {
              "description": "If present, executes commands using the configed shell '-c' option.",
              "$ref": "#/$defs/WrappingShell"
            },
            "type": {
              "type": "string",
              "const": "process"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "cmd"
          ]
        },
        {
          "description": "Spawns a managed container using `docker run --rm` (or `podman run --rm` via `engine`).",
          "type": "object",
          "properties": {
            "args": {
              "description": "Arguments passed to the container's entrypoint.",
              "type": "string"
            },
            "engine": {
              "description": "Container engine used to run the image (`docker` or `podman`, default `docker`).",
              "$ref": "#/$defs/ContainerEngine"
            },
            "env": {
              "description": "Environment variables passed to the container (a `KEY: value` mapping, rendered as `--env`).",
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            "image": {
              "description": "The container image reference to run.",
              "type": "string"
            },
            "ip": {
              "description": "Static IP address assigned to the container on the bridge network (rendered as `--ip`, empty values ignored).",
              "type": "string"
            },
            "mounts": {
              "description": "Mounts attached to the container (rendered as `--mount type=<type>,source=<source>,target=<target>[,readonly][,<opts>]`).",
              "type": "array",
              "items": {
                "$ref": "#/$defs/DockerMountConfig"
              }
            },
            "name": {
              "description": "Container name passed as `--name` (defaults to the enclosing process name).",
              "type": "string",
              "pattern": "^[A-Za-z0-9-_]+$"
            },
            "network": {
              "description": "Network to attach the container to (rendered as `--network`, empty values ignored).",
              "type": "string"
            },
            "opts": {
              "description": "Raw flags appended verbatim to the engine run command, after the rendered ones.",
              "type": "string"
            },
            "publish": {
              "description": "Ports published to the host (rendered as `-p`, empty entries ignored).",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "type": {
              "type": "string",
              "const": "docker"
            },
            "volumes": {
              "description": "Volumes attached to the container (rendered as `--volume`, empty entries ignored).",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "image"
          ]
        },
        {
          "description": "Spawns a local host binary attached inside a Docker bridge network namespace (`docker-intrude`).",
          "type": "object",
          "properties": {
            "cmd": {
              "description": "Host command string to execute within the target network namespace.",
              "type": "string"
            },
            "ip": {
              "description": "Static IP address assigned to the namespace container on the target bridge subnet.",
              "type": "string"
            },
            "network": {
              "description": "Target Docker network name (optional if exactly one network is defined in `docker_networks`).",
              "type": "string"
            },
            "shell": {
              "description": "If present, executes commands using the configed shell '-c' option.",
              "$ref": "#/$defs/WrappingShell"
            },
            "type": {
              "type": "string",
              "const": "docker-intrude"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "ip",
            "cmd"
          ]
        }
      ]
    },
    "RunManifest": {
      "description": "How the process command is specified in YAML.",
      "anyOf": [
        {
          "description": "Simple shell command string executed as a local OS process.",
          "type": "string"
        },
        {
          "description": "Explicit execution configuration specifying process type (`process`, `docker`, or `docker-intrude`).",
          "$ref": "#/$defs/RunKind"
        }
      ]
    },
    "WrappingShell": {
      "description": "Shell to be used to run commands.",
      "type": "string",
      "enum": [
        "bash",
        "zsh",
        "fish"
      ]
    }
  }
}